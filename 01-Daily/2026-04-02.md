## Modell-Kompatibilitätsprüfung (2026-04-02 10:27 UTC)
- OpenRouter Live-API geprüft für Llama 3.3, Llama 4 Maverick, Llama 4 Scout.
- Ergebnis für Lenards kostenoptimiertes Setup: Llama 3.3 70B Instruct (free) = optionaler GREEN-Kandidat als zusätzlicher Free-Fallback, aber kein klarer Ersatz für Nemotron free als Primary.
- Llama 4 Maverick / Scout sind in der Live-API nur paid sichtbar; mögliche :free-Seiten/Aliase sind derzeit nicht konsistent in der API auffindbar.
- Empfehlung: keine automatische Config-Änderung; nur beobachten, ob stabile :free-SKUs für Llama 4 auftauchen oder ob Scout/Maverick sich als klar besserer Mid-Tier-Fallback beweisen.

## Modell-Kompatibilitätsprüfung (2026-04-02 11:21 UTC)
- OpenRouter Live-API erneut geprüft gegen letzte bekannte Config und `model-providers.json`.
- Bestätigt: `meta-llama/llama-3.3-70b-instruct:free` ist live verfügbar und kompatibel.
- Abweichung: `openrouter/meta-llama/llama-4-maverick:free` und `openrouter/meta-llama/llama-4-scout:free` stehen lokal in `model-providers.json`, aber die OpenRouter Live-API listet aktuell keine `:free`-SKUs dafür.
- Zusätzliche Abweichung: `openclaw.json` erlaubt `openrouter/meta-llama/llama-4-maverick:free`, aber keinen Scout-Eintrag; beide wären aktuell ohnehin nicht stabil nutzbar, solange die Live-API keine Free-IDs liefert.
- Empfehlung: Llama 3.3 free höchstens als optionalen Free-Fallback beobachten; Llama 4 Maverick/Scout nicht in produktive Fallback-Chain aufnehmen, bis stabile Live-API-IDs und Verhalten bestätigt sind.

## Mission Control – Legacy Assignment Migration abgeschlossen (2026-04-02 21:52 UTC)
- Step B UI-/Board-Truth-Cut auf `assigned_agent` wurde sichtbar validiert; Board und API zeigen dieselbe `assigned_agent`-Wahrheit.
- Safe-Set-Migration ausgeführt: 126 Tasks auf `assigned_agent = assignee` normalisiert.
- Casing-only-Rest bereinigt: 3 `workerLabel`-Werte auf korrektes Casing normalisiert.
- `researcher` als gültiger Agent in Mission Control App ergänzt (`agents-data.ts`, `src/app/agents/page.tsx`); kein Datenwrite dafür nötig.
- Verbleibende Restmenge wurde bewusst als Manual-Review-Bucket eingefroren: echte Ownership-Konflikte, ambige Sonderfälle, Fixture `svc_phase1_fixture_001` unverändert.
- Abschlussstatus dokumentiert in `mission-control/tmp/legacy-assignment-safe-set-report.json` und `mission-control/CRON_REVIEW_SUMMARY_2026-04-02.md`.

## Mission Control – Agents Live Board + Deploy-Fix (2026-04-02 22:58 UTC)
- Agents Live Board umgesetzt: `/api/agents/live`, Live-Agents-UI, Ping-Flow, Collapse/Expand, `activityPulse`, Ping-Feedback mit Channel-Suffix.
- Live-Ping erfolgreich validiert über `/api/discord/send` mit `ok: true`.
- Root cause des ungestylten/kaputten Live-Boards war ein Deploy-/Runtime-Problem: paralleles Build/Start bzw. instabiler Startpfad konnte `.next`-Artefakte während laufender Runtime invalidieren.
- Fehlannahme korrigiert: `prestart`-Kill auf Port 3000 in `package.json` war in dieser Umgebung instabil und wurde wieder entfernt.
- Stabiler Standardpfad ist jetzt `mission-control/deploy.sh` für Build + Port-3000-Kill + frischen Start; `npm run build` bleibt für Build-only.
- Nach Deploy-Fix live verifiziert: `/agents` liefert 200 und das referenzierte CSS-Asset unter `/_next/static/css/...` liefert wieder 200.

## Mission Control – Agents Live Board + Deploy Workflow + Taskboard Truth Follow-up (2026-04-02 23:33 UTC)
- Agents Live Board umgesetzt und live validiert: `/api/agents/live`, Live-Agents-UI, Ping-Flow inkl. Channel-Feedback, Collapse/Expand pro Agent-Row, `activityPulse`-Badge.
- Wichtige Deploy-Erkenntnis: paralleles `next build` gegen laufendes `next start` kann `.next`-Artefakte inkonsistent machen und zu ENOENT/500 bei CSS/Server-Dateien führen.
- Korrektur: `prestart`-Kill (`fuser -k 3000/tcp`) war in dieser Umgebung instabil und wurde wieder verworfen; stabiler Pfad ist jetzt `mission-control/deploy.sh` für Build+Restart, `npm run build` nur ohne Restart.
- README im Mission Control Workspace dokumentiert den stabilen Deploy-Workflow mit `bash deploy.sh`.
- Taskboard-Hygiene auf `mission-control/data/tasks.json` angestoßen und ausgeführt: Backup `tasks.json.bak.hygiene-2026-04-02` angelegt, done-Pattern-Fälle und ältere done-Duplikate auf `archived` gesetzt; andere Status sollten unberührt bleiben.
- Offener fachlicher Folgepunkt identifiziert: "Projection/Intelligence Truth Fix" für Taskboard. Root cause: Board-Projektion/Intelligence behandelt bestimmte dispatched/completed/result-Fälle noch nicht als operative Wahrheit; dadurch erscheinen inkonsistente Karten weiter als aktive/idle/relevante Arbeit.
- Nutzerwunsch: neuen Task "Projection/Intelligence Truth Fix" ins Board eintragen.
