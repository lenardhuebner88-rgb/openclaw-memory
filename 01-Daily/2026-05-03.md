# 2026-05-03

## Mission Control – Models Health Fix + Runtime-Normalisierung (2026-05-03 23:58 CEST)

### Kontext
- Während `next build` war Mission Control kurz nicht auf `:3000` erreichbar.
- Ziel war: `/api/models/health` robust machen (kein falsches Hard-Fail bei fehlender lokaler Secret-Source) und Runtime sauber auf den kanonischen Startpfad normalisieren.

### Umsetzung
- `/api/models/health` angepasst:
  - `deepseek` und `anthropic` ohne lokale Secret-Source werden als `skipped` markiert.
  - `summary` enthält jetzt `ok`, `failed`, `skipped`.
- MiniMax-Key/Endpoint wurde zuvor live validiert (Anthropic-kompatibler MiniMax-Pfad, HTTP 200).
- Runtime-Normalisierung über kanonischen Weg ausgeführt:
  - `mc-restart-safe 180 runtime-normalization`

### Live-Gates (verifiziert)
- `systemctl --user status mission-control.service` => `active (running)`
- Service läuft systemd-managed auf Port `3000` (`next-server`, `MainPID=339194` zum Prüfzeitpunkt).
- `GET http://127.0.0.1:3000/api/models/health` liefert:
  - `minimax-M2.7-HS`: `ok=true` (echter Health-Ping, Latenz gesetzt)
  - `deepseek-V3.2`: `ok=true`, `skipped=true`, `reason=no-local-secret-source`
  - `anthropic-opus`: `ok=true`, `skipped=true`, `reason=no-local-secret-source`
  - `summary`: `ok=1`, `failed=2`, `skipped=2`

### Ergebnis
- Status: `done`
- Der Fix ist live wirksam; `deepseek/anthropic` zählen ohne lokale Secret-Source nicht mehr als harte `401`-Fehler.
