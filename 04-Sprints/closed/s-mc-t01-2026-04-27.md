# S-MC-T01 — Critical Bug Triage Sprint

**Status:** ✅ DONE
**Datum:** 2026-04-27
**Sprint-Master:** Atlas
**Owner:** Forge (T1–T4), Pixel (T5)

---

## Ergebnis

| Task | Titel | Status | Agent | Letzte Aktivität | Commit |
|------|-------|--------|-------|-----------------|--------|
| T1 | /automate Dead-Link Resolution | ✅ done | sre-expert | 2026-04-28 15:11 | Retry: Route war bereits funktional |
| T2 | Mermaid-Render in /ops fixen | ✅ done | sre-expert | 2026-04-28 12:48 | Mermaid v11 API-Änderung: `await window.mermaid.render()` |
| T3 | DATA: FALLBACK Root-Cause + Fix | ✅ done | sre-expert | 2026-04-28 15:56 | Fix via Commit 5643738 (Retry: Auto-Pickup-403-Blockade überwunden) |
| T4 | Cost Source-of-Truth Konsolidierung | ✅ done | sre-expert | 2026-04-28 15:20 | Kein Code-Change nötig; beide Endpunkte bereits auf identischem getDailyCost() |
| T5 | ErrorBoundary global einziehen | ✅ done | frontend-guru | 2026-04-28 12:40 | GlobalErrorBoundary in layout.tsx |

---

## T3 DATA FALLBACK — Verifikation

**T3 DATA FALLBACK** (Task `359ed4a1`):
- Root-Cause: Sub-Agent Fix via Commit 5643738 am 2026-04-27
- Retry-Task (`916d45c6`-T3) wurde durch Auto-Pickup-403-Bug (HTTP 403 beim Receipt-Endpoint) blockiert
- Retry erfolgreich am **2026-04-28 15:56** — Sprint damit vollständig durchlaufen
- Sprint insgesamt sauber: Alle 5 Tasks `done`, kein `failed` (außer T8 Chaos-Test als Operator-Entscheid)

---

## Verify Commands

```sh
# T1 — /automate
curl -s http://localhost:3000/automate | grep -c "200"  # → 1

# T2 — /ops Mermaid
curl -s http://localhost:3000/ops | grep -c "svg"       # → >0

# T3 — DATA FALLBACK (kein Fallback-Card im UI mehr)
curl -s http://localhost:3000/api/tasks | jq '[.tasks[] | select(.title | contains("DATA"))]'

# T4 — Cost SoT
curl -s http://localhost:3000/api/costs | jq '.today'
curl -s http://localhost:3000/api/analytics | jq '.kpis.dailyCost'
# Beide Werte identisch

# T5 — ErrorBoundary
curl -s http://localhost:3000 | grep -c "GlobalErrorBoundary"  # → 1
```

---

## Build-Status

`npm run build` → ✅ green (alle Sub-Tasks bestätigt)

---

## Offene Punkte / Follow-ups

- **403er Auto-Pickup Bug** (P1): Separater Task `7f4cdd21` — dispatched 2026-04-28 16:16 an Forge
- **T8 Chaos-Test**: Operator-Entscheid = bestanden ohne Re-Run (S-RELIAB-P0 dokumentiert)

---

## Archivierung

- Sprint-Plan ursprünglich: `vault/04-Sprints/planned/2026-04-27_s-mc-t01-critical-bug-triage.md`
- Geschlossen am: 2026-04-28
- Archivierungsgrund: Alle 5 Subs done, Build grün, Sprint erfüllt
