- Discord exec-approval flow is now accepted as working after a real end-to-end proof: approval request appeared in Discord, Allow once click was processed, approval resolved, blocked exec resumed, and host marker artifacts `/tmp/live-discord-click-proof` and `/tmp/live-discord-click-proof/marker` were created.
- Root cause of the broken Discord approval resume path: in `/home/piet/.openclaw/lib/node_modules/openclaw/dist/gateway-cli-DlnlX7IW.js` the `exec.approval.resolve` path previously called `forwarder.handleResolved(...)` fire-and-forget instead of awaiting it, so approval could be marked resolved while the execution continuation was still detached/droppable. Minimal fix: await the forwarder continuation before returning success.
- Active Discord approval config now documented: `channels.discord.execApprovals.enabled = true`, approver limited to `207500364776341504`, target=`channel`, cleanupAfterResolve=`true`. Operator runbook written to `mission-control/docs/discord-approval-runbook.md`.
