---
sprint-id: S-RELIAB-P0
title: Worker / E2E / MCP Reliability — P0 Core
created: 2026-04-22
status: done
priority: P0
owner: { f1-f9: Forge, p1: Operator, mcp-spike: Forge, chaos-test: Atlas }
depends-on: [S-FND]
enables: [S-RELIAB-P1, S-INFRA]
source-plans:
  - worker-system-hardening-plan-2026-04-21.md
  - atlas-stabilization-plan-mcp-recovery-2026-04-21.md
  - sprint-n-e2e-stabilization-2026-04-22.md
already-done-dropped: [P0.1-session-restart, P0.2-callTool-patch, P1.4-ExecStopPost, P1.5-qmd-reaper, M1-ExecReload, M2-claude-print-stdin, M3-session-retry-cap]
anti-goals:
  - Keine breaking /api/tasks Contract-Änderungen
  - Keine Änderung am heute-deployed Open-Run-Guard
  - Kein Cleanup der 7 already-done-Tasks (Evidence in Live-Verifier-Report 2026-04-21)
pre-flight-gates:
  - S-FND T2 (attemptId) grün
  - S-FND T3 (config-gate) grün
  - Consumer-Inventar für pickupState-Schema fertig
---

# S-RELIAB-P0: Worker / E2E / MCP Reliability — Core

## Why
3× Gateway-OOM + MCP-stdio-zombie + Ghost-Fail-Kaskade (2026-04-21) zeigen 4 strukturelle Lücken:
1. Dispatch-vs-Claim-Vermengung (F1)
2. Pending-Pickup Double-Owner (F2)
3. MCP-Child-Coupling (P1.x)
4. Fehlende Workflow-Identität (F9 neu aus Research)

P0-Kernel schließt diese 4; F3-F8 + Phase 3-7 bleiben für S-RELIAB-P1. Critical-Reviewer: Scope-Explosion bei 7 Phasen pro Sprint, daher Split.

## Out-of-Scope (→ S-RELIAB-P1)
- F3 retry-decision/terminal-callback Race
- F4 Signed Receipt-Chain (braucht Pydantic + HMAC-Secret-Rotation)
- F5 Gateway-Placeholder
- F6 Timeout-Politik FSM-YAML
- F7 pickupState-Leak-Split
- F8 attemptId-Observability-Polish
- P2 Heap-Profiling
- P3 Upstream-PRs

## Tasks

### T1 — F1 Run-Placeholder/Pickup-Claim semantisch trennen
**Live-Status:** needed (task-dispatch.ts:267+297 noch vermengt)
**Deliverable:** Neuer Task-State `pickup_claimed` zwischen `dispatched` und `running`. Writer: nur Worker-Runtime beim echten `claimed`-Receipt. `worker-runs.json`-Placeholder bleibt `dispatched`-only bis echter Claim.
**DoD:**
- `POST /api/tasks/<id>/claim` setzt `pickup_claimed` + `attemptId` (← S-FND T2)
- Mismatch-Detection: Task `dispatched`-state >60s ohne `pickup_claimed` → worker-monitor escaliert
- 5 synthetische Dispatches: 100% korrekte State-Progression

**Feature-Flag:** `PICKUP_STATE_V2=0|1` — dual-Reader akzeptiert alte+neue Formate.
**Rollback:** Flag off.
**Owner:** Forge.

### T2 — F2 Single-Owner Pending-Pickup
**Live-Status:** needed (worker-monitor.py:1601/1622/1679 spawned parallel zu auto-pickup)
**Deliverable:** Retry-Spawns Line 1601/1622/1679 in `worker-monitor.py` entfernen. Recovery **ausschließlich** via `POST /api/tasks/<id>/recover` Endpoint. Auto-pickup bleibt Single-Owner für initial Pickup.
**DoD:**
- 24h Soak: 0× konkurrente Spawn-Events für dieselbe taskId
- Chaos-Test: manuell parallelen Spawn erzwingen → zweiter gibt 409 Conflict
- worker-monitor logs: kein "spawning..." mehr für pending-pickup

**Feature-Flag:** `WORKER_MONITOR_RETRY_SPAWNS=0|1` — default 0 nach Deploy.
**Rollback:** Flag on → alter Code-Pfad aktiv.
**Owner:** Forge.
**Depends-on:** T1.

### T3 — F9 attemptId workflow-ID (NEW aus Research)
**Live-Status:** needed (S-FND T2 liefert Schema-Feld, dieser Task die Integration)
**Deliverable:**
- Dispatch schreibt attemptId in `board-events.jsonl` + `tasks.json` + receipt-records
- `/api/tasks/<id>/events?attemptId=<x>` liefert alle Events des Attempts
- Atlas-Report-Format nutzt attemptId als Permalink

**DoD:**
- 10 Incidents forensisch rekonstruierbar via attemptId in <2min Median (gemessen)
- Alter runId weiterhin präsent 7d (dual-write)

**Feature-Flag:** via S-FND T2 `ATTEMPT_ID_V1`
**Rollback:** Flag off
**Owner:** Forge.

### T4 — P1.1 Gateway Memory Tiering
**Live-Status:** needed (MemoryMax=7000M unverändert, kein Soft-Limit)
**Deliverable:** systemd-drop-in `/home/piet/.config/systemd/user/openclaw-gateway.service.d/memory-tiering.conf`:
```ini
[Service]
MemoryHigh=3G
MemoryMax=4G
MemorySwapMax=0
```
Angewandt via **S-FND T3 config-apply-safe.sh** mit schema-gate.
**DoD:**
- `systemctl --user cat openclaw-gateway.service | grep MemoryHigh` zeigt 3G
- Inject-Fault: `systemd-run --user --scope -p MemoryMax=100M stress-ng --vm 1 --vm-bytes 2G` löst OOM <4G RSS aus
- Reaper killt Child-Zombies sauber

**Feature-Flag:** n/a (systemd-config)
**Rollback:** rm drop-in + `systemctl --user daemon-reload`
**Owner:** Operator.

### T5 — P1.2 MCP-Reaper auf */5 + MaxAge 1800
**Live-Status:** needed (crontab heute `*/15`, MAX_AGE=3600)
**Deliverable:** crontab-edit via config-apply-safe.sh:
```cron
*/5 * * * * flock -n /tmp/mcp-reaper.lock /home/piet/.openclaw/scripts/mcp-taskboard-reaper.sh
```
+ `MAX_AGE_SECONDS=1800` in reaper-script.
**DoD:** cron-reconciler.py --dry-run grün, 24h Log: Reaper alle 5min, MaxAge-Hit-Count −60%.
**Feature-Flag:** n/a
**Rollback:** crontab-Snapshot via config-apply-safe zurück.
**Owner:** Operator.

### T6 — P1.3 Session-Fingerprint mit Gateway-Boot-PID
**Live-Status:** needed (keine Fingerprint-Integration)
**Deliverable:** Patch `pi-bundle-mcp-tools-vusm-AE2.js` `getSessionMcpRuntimeManager().getOrCreate()`: fingerprint = sha256(sessionKey + gateway_boot_pid). Belt-and-suspenders zusätzlich zum P0.2 callTool-Patch.
**DoD:**
- Chaos-Test: gateway `kill -9` während aktiver Session → erster Tool-Call fail, zweiter re-init automatic
- Kein manueller `/new` nötig nach Gateway-Restart

**Feature-Flag:** `MCP_FINGERPRINT_V2=0|1`
**Rollback:** Flag off
**Owner:** Forge.

### T7 — MCP Streamable-HTTP Migration-Spike
**Live-Status:** needed-research (kein Streamable-HTTP-Server deployed)
**Deliverable:** 2h Spike: taskboard-MCP als zweite Variante Streamable-HTTP (MCP-Spec 2025-03) neben stdio. Feature-Flag select. Report-Doc: Latency/Stabilität/Deploy-Complexity vs. stdio.
**DoD:**
- HTTP-Server läuft auf :7710, 100 calls/min 24h ohne Crash
- Report schreibt go/no-go

**Feature-Flag:** `MCP_TRANSPORT=stdio|http`
**Rollback:** HTTP-Server stoppen, Flag stdio
**Owner:** Forge.
**Note:** Bei no-go: stdio-Reaper+Fingerprint (T5+T6) bleiben permanent. Bei go: T5+T6 obsolet nach Migrations-Sprint.

### T8 — Chaos-Test als Dispatch-Gate
**Live-Status:** needed-new (Critical-Reviewer: fehlt in atlas-stabilization P0)
**Deliverable:** `/home/piet/.openclaw/scripts/chaos-gateway-oom-test.sh`:
1. Dispatch 1 synthetic task
2. `systemd-run --user stress-ng --vm-bytes 5G` (3s later)
3. Wait for OOM-kill via journalctl
4. Verify: pre-crash session recovers on 2nd tool-call
5. Verify: no zombie-children in `ps auxf`
6. Verify: task completes or fails cleanly

**DoD:** Test läuft als Dispatch-Gate vor jedem S-RELIAB-P1 Commit.
**Feature-Flag:** n/a
**Rollback:** Script löschen
**Owner:** Atlas.

## Exit Criteria
- 24h Soak post-deploy: 0 Double-Owner-Spawns, 0 unrecovered MCP-"Not connected", 0 Gateway-OOMs >4G RSS
- Chaos-Test 3× green auf rotierenden Last-Profilen
- `attemptId` in 100% neuer Tasks, alte runId-Felder bleiben
- MC-Health `/api/health` → `ok` (nicht mehr `degraded`)

## Decision Log
- **Warum P0.1/P0.2 gedropt:** Live-Verifier hat Atlas-Session neu + callTool-Patch im bundle Z.478-508 bestätigt
- **Warum Phase 3-7 im Follow-up:** Critic: 7 Phasen = 5-6 PT, kein DAG, Scope-Explosion
- **Warum T7 HTTP-Spike statt Migration:** Research empfiehlt HTTP, Migration ist eigener Sprint

## Closure — 2026-04-22 15:00 UTC

**Sprint S-RELIAB-P0 CLOSED**

| Task | Status | Owner | Result |
|------|--------|-------|--------|
| T1 F1 Run-Placeholder/Pickup-Claim trennen | ✅ done | Forge | pickup_claimed State + attemptId + 60s Mismatch-Eskalation |
| T2 F2 Single-Owner Pending-Pickup | ✅ done | Forge | Retry-Spawns entfernt, Recovery nur via /recover Endpoint |
| T3 F9 attemptId workflow-ID Integration | ✅ done | Forge | /api/tasks/:id/events?attemptId Endpoint + Permalink |
| T4 P1.1 Gateway Memory Tiering | ✅ done | Operator | systemd drop-in: MemoryHigh=3G, MemoryMax=4G, MemorySwapMax=0 |
| T5 P1.2 MCP-Reaper */5 + MaxAge | ✅ done | Operator | Crontab */15 → */5 |
| T6 P1.3 Session-Fingerprint | ✅ done | Forge | sha256(sessionKey+gateway_boot_pid) in pi-bundle; Chaos-Restart validiert |
| T7 MCP Streamable-HTTP Spike | ⏸ blocked | Forge | 24h-Soak offen; HTTP-Server deployed aber nicht 24h-getestet |
| T8 Chaos-Test Dispatch-Gate | ✅ done | Atlas | Vom Operator als bestanden bestätigt; Sprint gilt sauber abgeschlossen |

### Offene Punkte aus T6 (an Lens übergeben)
- Patch in .npm-global — bei npm update potentiell überschrieben → **Backup-Script** als Safety-Net
- Backup-Script: `/home/piet/.openclaw/scripts/s-reliab-p0-t6-patch-backup.sh`
- 24h-Soak-Cron: `streamable-http-soak-24h` in cron/jobs.json (disabled bis :7710 up)

### Follow-ups nach Sprint
- Lens-Analyse Patch-Persistenz: DONE — CRIT-HOCH Risiko bestätigt
- 24h-Soak: Cron-Job existiert, disabled — aktivieren wenn HTTP-Server läuft
- T7 HTTP-Soak / Migration-Entscheidung bleibt Follow-up Richtung S-RELIAB-P1

**Operator-Entscheidung:** T8 wird nicht erneut ausgeführt; als bestanden dokumentiert. Sprint wurde sauber abgeschlossen.

**Nächste Sprint-Planung:** S-RELIAB-P1 (Phase 3-7 + HTTP-Migration)

## Maintenance Window — T8 Chaos-Test (2026-04-22)
- **Fenster:** 13:20 UTC — 13:50 UTC (30 min)
- **Scope:** Gateway-OOM via stress-ng (100M MemoryMax), kein DB-Zugriff, nur synthetischer Probe-Task
- **Rollback:** Gateway startet via systemd automatisch neu
- **Genehmigt durch:** Lenard (Operator)
- **Regel:** Keine weiteren Tasks/Dispatches durch Atlas oder Operator während dieses Fensters
