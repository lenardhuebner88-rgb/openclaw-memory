---
sprint-id: S-INFRA
title: Sprint-K Restpunkte — H6 Receipt-Lifecycle + H7 Rollback + H6'L5 FSM
created: 2026-04-22
status: planned
priority: P1
owner: { h6-l1: Atlas, h6-l3: Forge, h6-l4-hook: Forge, h7b-rollback: Operator, h6-l5-fsm: Forge }
depends-on: [S-RELIAB-P0]  # T1 pickup_claimed-state blocks H6 L3, gemeinsamer Worker-Monitor-Patch
enables: []
source-plans: [sprint-k-infra-hardening-plan-2026-04-19.md]
already-done-dropped: [H6-L2-auto-transition, H7-mc-restart-safe, H10-L1-cleanup, H10-L2-L5-superseded-by-Sprint-M]
anti-goals:
  - Keine Agent-Prompt-Revolutionen (nur append in H6 L1, keine Section-Änderung)
  - Keine Re-Open von Sprint-M (H10 Rest)
  - Keine H7a-Änderungen (mc-restart-safe done)
pre-flight-gates:
  - S-RELIAB-P0 T1 (pickup_claimed state) grün
  - S-RELIAB-P0 T2 (single-owner) grün
  - Plan-Doc auf 2026-04-22 aktualisiert (Critic!)
---

# S-INFRA: Sprint-K Restpunkte — Receipt-Lifecycle + Rollback

## Why
Sprint-K Live-Status (2026-04-21):
- H6 L2 Auto-Transition: DONE (auto-transition-watcher.py existiert)
- H7 mc-restart-safe: DONE (/home/piet/.local/bin/)
- H10 L1 Cleanup: DONE
- H10 L2-L5: **superseded by Sprint-M M6+M7**

Rest:
- H6 L1 Prompt-Preamble (Agent-Verify)
- H6 L3 Worker-Monitor Extension (Overlap mit S-RELIAB! koordinieren)
- H6 L4 Commit-Signal: **dropped aus Research** → ersetzt durch Post-Commit-Hook (Critic + Research)
- H7b Saga-Rollback für mc-restart-safe (Research)
- H6'L5 FSM-Stall für `assigned` (Research, neu)

## Out-of-Scope
- H10 L2-L5 (superseded)
- H7a mc-restart-safe (done)
- H6 L2 (done)
- Upstream-PR-Tracks

## Tasks

### T1 — H6 L1 Prompt-Preamble verifizieren
**Live-Status:** needs-verify (openclaw.json agents.list nicht geprüft)
**Deliverable:** 4 Sub-Agent-Prompts (Atlas, Forge, Pixel, Lens) bekommen appended Preamble (nur append, keine Section-Änderung):
- "Vor jedem work-step: send progress-receipt via /api/tasks/<id>/receipt"
- "Bei /fail: explicit reason_code + evidence_ref"

Cross-Check mit S-CTX CE1-Baseline — geschätzter Token-Overhead ~200-500 tokens.

**DoD:**
- openclaw.json agents.list 4× preamble present
- S-CTX Baseline Delta ≤X% (wird in S-CTX CE1 gemessen)

**Feature-Flag:** n/a (prompt-change)
**Rollback:** Diff revert in openclaw.json (S-FND T3 config-apply-safe)
**Owner:** Atlas.

### T2 — H6 L3 Worker-Monitor Receipt-Discipline-Extension
**Live-Status:** partial (worker-monitor hat pending-pickup handling, aber nicht assigned-without-receipt)
**Deliverable:** `worker-monitor.py` erweitern:
- State `running` >2min ohne `progress`-Receipt → Escalate
- State `assigned` >60s ohne `claimed`-Receipt → Escalate (FSM-Edge, siehe T5)

**Coordination:** Overlap mit S-RELIAB-P0 T2 (Single-Owner). **Gemeinsam implementieren**, nicht 2 parallele Patches.

**DoD:**
- 5 synthetische Stall-Scenarios: alle erkannt + escalated
- Kein False-Positive auf healthy workers

**Feature-Flag:** `RECEIPT_DISCIPLINE_ENFORCE=0|1`
**Rollback:** Flag off
**Owner:** Forge.
**Depends-on:** S-RELIAB-P0 T2 merged.

### T3 — H6 L4' Post-Commit-Hook statt Keyword-Match
**Live-Status:** needed-new (Critic + Research: Drop H6 L4 commit-signal → ersetzen durch deterministische Variante)
**Deliverable:** Git-post-commit-Hook in `/home/piet/.openclaw/workspace/mission-control/.git/hooks/post-commit`:
```bash
#!/bin/bash
# Reads ATLAS_TASK_ID env-var (set by agent-sandbox)
# Parses [task:<uuid>] in commit-message as fallback
# POSTs receipt to /api/tasks/<id>/receipt with commit-SHA
```
Convention: Agent-Runtime setzt `ATLAS_TASK_ID` vor `git commit`. Wenn nicht gesetzt + kein `[task:<uuid>]` → Hook noop (kein False-Positive).

**DoD:**
- 5 Commits mit ATLAS_TASK_ID: receipt auto-posted
- 5 Commits ohne: kein receipt, kein Error
- False-Positive-Rate 0% (deterministisch)

**Feature-Flag:** `POST_COMMIT_RECEIPT=0|1`
**Rollback:** Hook remove
**Owner:** Forge.

### T4 — H7b Saga-Compensation für mc-restart-safe
**Live-Status:** needed-new (Research-A.8)
**Deliverable:** `mc-restart-safe` Wrapper erweitert:
- Nach 60s Wartet auf MC healthz
- Wenn nicht up: `systemctl --user start mission-control@previous.service` (zweite Unit mit Last-Known-Good-BUILD_ID)
- Discord-Alert: "MC-Restart failed, rolled back to previous"

Prerequisite: `mission-control@previous.service` Unit-File.

**DoD:**
- Inject-Fault: mc-restart-safe mit kaputtem BUILD_ID → auto-rollback in <120s
- Vorherige Version wieder healthy

**Feature-Flag:** n/a (script-behavior)
**Rollback:** mc-restart-safe Original-Version
**Owner:** Operator.

### T5 — H6'L5 FSM-Stall-Detection für `assigned`-State
**Live-Status:** needed-new (Research-A.3)
**Deliverable:** `worker-fsm.yaml`:
```yaml
states:
  assigned:
    stall_conditions:
      - max_duration: 60s
        progress_delta_required: false  # nur state-change reicht
        action: escalate
  running:
    stall_conditions:
      - max_duration: 120s
        progress_delta_required: true
        action: escalate
```
Integration in `worker-monitor.py` (ersetzt hardcoded Dict).

**DoD:**
- 3 FSM-Violations injected: alle erkannt
- 24h Soak: 0 False-Positive

**Feature-Flag:** `FSM_STALL_DETECTION=0|1`
**Rollback:** Flag off — altes Timer-Dict aktiv
**Owner:** Forge.

## Exit Criteria
- T1 Preamble in 4 Agents, S-CTX-Delta ≤10%
- T2+T5 FSM + Receipt-Discipline 24h Soak grün
- T3 Post-Commit-Hook 100% deterministisch
- T4 Saga-Rollback-Test grün

## Decision Log
- **H6 L4 gedropt:** Commit-Signal-Heuristik 5% False-Positive zu hoch bei 40 Commits/Tag
- **H10 L2-L5 gedropt:** Superseded by Sprint-M M6b (Memory-Orchestrator)
- **H6 L3 + S-RELIAB-P0 T2 coordination:** Worker-Monitor-Patches beißen sich, gemeinsam implementieren
