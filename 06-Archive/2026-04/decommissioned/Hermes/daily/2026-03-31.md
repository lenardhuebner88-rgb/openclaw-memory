- Mission Control task flow contract unified on live taskboard store/API.
- Added flat contract fields for dispatchState, dispatchTarget, dispatchChannelId, resultSummary/resultDetails/resultAt and normalized legacy tasks on read/write.
- `/api/tasks/[id]` is now the single writeback path for assignment/status/result metadata; dispatch route writes back through the same store contract.
- `/taskboard` switched to dynamic rendering so board-visible state now reflects live API changes in production.
- Validated live on port 3102: create -> queue -> dispatch -> writeback -> GET/taskboard reflection worked end-to-end.
- Mission Control overview now includes a Board Intelligence panel with ranked attention cards, owner/reason/source/freshness signals, board-signal chips, and a single next-best action fed from live overview intelligence output.
- Validation: `npm run build` passed after the UI integration; existing Turbopack/root and chart-size warnings remain non-blocking and pre-existing.
- Added taskboard intelligence core: deterministic scoring from live task fields (status, priority, dispatch state, update age, result capture, auto-source) plus attention queue, board signals, confidence score, and next-best-action rule wired into /taskboard UI.
- Pulse runtime validation of Board Intelligence scenarios on live state: 4/5 scenarios passed; only the "unassigned task" scenario failed.
- Exact BIL validation gap: tasks with missing/unassigned owner are not surfaced as a dedicated missing-owner signal, confidence stays too high (~84%), and next-best-action incorrectly recommends starting backlog work instead of first assigning ownership.
- Required follow-up fix for Board Intelligence: add explicit missing-owner attention/signal handling and make next-best-action prefer ownership capture/assignment before backlog start when assignee is blank or `unassigned`.
- Mission Control UI regression root cause was confirmed as runtime/source mismatch: port 3000 served stale repo `/home/piet/.openclaw/workspace-frontend-guru/mission-control-src` with old shell/build, not the canonical `/home/piet/.openclaw/workspace/mission-control` build.
- Frontend-Guru restored the Task tab shell in the canonical repo: proper dashboard shell, hero/status section, KPI cards, Now/Next/Later spotlight columns, clearer filter panel, improved task cards, and contained activity rail; `npm run build` passed.
- Deterministic task-flow failure map was verified: previous trust issues came from split-brain between live runtime and current source, schema divergence (`assignedTo` vs `assignee`, `ready/open/in_progress` vs backlog/review/in-progress), missing live dispatch/move/activity routes on the stale runtime, and stale prerendered board output.
- Verified live operating contract after fixes: Mission Control now uses a unified live task contract with explicit dispatch/result metadata and live board reflection; creation, assignment, dispatch, result writeback, and board rendering were validated end-to-end on the fixed live instance.
- Learning loop baseline was established in `memory/learning-log.json`: first persisted reusable problem pattern is `MISSING_FIELDS` with successful schema-normalization fix; repeated evaluation now correctly returns strategy=`reuse` for that problem type.
- Mission Control UI stability pass: fixed broken overview quick-jumps, made MissionShell spacing responsive, stabilized taskboard drag-drop by reverting failed optimistic moves, made filtered empty-lane states explicit, and constrained activity rail height to avoid layout collisions. Build green; live routes / and /taskboard returned 200.
- Mission Control approve/dispatch mapping fix: `src/lib/task-dispatch.ts` now normalizes role labels and agent-id aliases (including `Frontend-Guru`/`frontend-guru`, direct ids, and case variants) before resolving Discord channel bindings. Live proof on port 3000: task `cb4e1368-4598-483c-94d2-5fd984f12ee4` (`CHANNEL MAP LIVE VALIDATION`) initially failed `/api/tasks-approve` with `Channel mapping not found`, then succeeded after rebuild/restart and reflected as `dispatchState=dispatched` on `/api/tasks` and `/taskboard`.
- Mission Control runtime incident (31.03.2026): dispatch/readback inconsistency investigation evolved into a served-code-path mismatch incident. Proven facts: authoritative listener on :3000 was `next-server` from `next start`; old user service restart path caused EADDRINUSE noise and was disabled/stopped. GET /api/tasks matched canonical disk, so board/UI was not the primary break for the latest failing validation tasks.
- Proven on 31.03.2026: authoritative runtime served compiled `.next/server/...` artifacts that did not reflect edited instrumentation source for `/api/tasks/[id]/dispatch`; no `DISPATCH_ROUTE_HIT` / `DISPATCH_TASK_CALLED` / `WRITE_TASKS_CALLED` markers appeared, and served route artifact lacked injected marker strings. Therefore request-path instrumentation remained untrusted until runtime alignment is achieved.
- Loop-breaking orchestration state (31.03.2026): Stage 1 runtime alignment was started. Actions completed: port 3000 listener conflicts reduced, `.next` removed and rebuilt once, non-authoritative systemd restart path disabled. However, after restart attempts the new authoritative listener still appeared to serve artifacts that did not match edited source. Stage 1 remained incomplete because `served artifact == edited source` was not yet proven.
- Operational rule reinforced (31.03.2026): no phase may remain "defined but not executed"; after a phase is defined and no active executor is working on it, Atlas must immediately create one Task Contract, assign one owner, and dispatch execution. Also reinforced: gate-fail result = reject + re-dispatch; no batch chaos during root-cause repairs.
- Architecture decision (31.03.2026): choose `2-BOT` model for Discord/OpenClaw ops. Atlas Bot = orchestration/control surface; Worker Bot = visible execution surface. Discord remains interface only; Mission Control Task Board remains single source of truth; agent identity must live in runtime/session/owner fields, not Discord channel or bot appearance.
