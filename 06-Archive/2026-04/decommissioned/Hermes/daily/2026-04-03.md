# 2026-04-03 — Stabilisierung Session

## Eintrag

### Mission Control / Agent-Stabilisierung
- Session-Handover abgearbeitet und offene Systemblöcke sequenziell geprüft.
- `efficiency-auditor` (Lens) von fehleranfälligem Modellpfad auf `openrouter/auto` umgestellt; Smoketest grün.
- `researcher` ebenfalls auf `openrouter/auto` umgestellt; Agent läuft wieder ohne `LiveSessionModelSwitchError`.
- Zusätzliche Modellbereinigung durchgeführt:
  - `quick` → `openrouter/auto`
  - `projekte` → `openrouter/auto`
  - `prompt-optimizer` → `openrouter/auto`
- HEARTBEAT.md bereinigt: alle veralteten `"status": "backlog"` in Auto-Task-Snippets auf `"status": "draft"` geändert.
- Infra-Befund: Tailscale auf Host `huebners` derzeit nicht installiert / nicht nutzbar; separater Board-Task für Forge angelegt.
- Weiterer Board-Task angelegt: GPT-5.4 Kosten-Diskrepanz durch Lens prüfen.

### Mission Control – spätere Session: Lifecycle-, UI- und Audit-Härtung
- Kanonische Task-Lifecycle-Routen live verifiziert:
  - create → `POST /api/tasks`
  - assign → `PATCH /api/tasks/[id]`
  - dispatch → `POST /api/tasks/[id]/dispatch`
  - result/writeback → `POST /api/tasks/[id]/receipt`
  - completion → `PATCH /api/tasks/[id]`
- Wichtige Erkenntnis: `PUT /api/tasks/[id]/move` ist **nicht** der kanonische Assign-Pfad; `draft -> ready` bleibt im öffentlichen Task-Status `draft`, weil die Board-State-Matrix `ready` intern auf persistierten Status `draft` abbildet.
- Reale Lifecycle-Validierung ergab: `receipt(stage=result)` hält Task-Status sichtbar auf `in-progress`; erst `PATCH status=done` schließt sauber ab.
- UI-/Runtime-Härtetest-Regel eingeführt und praktisch verwendet:
  1. Pixel implementiert Minimal-Fix
  2. Forge prüft Live-/Build-/Serve-Pfad
  3. Lens audited das Ergebnis gegen PASS/FAIL-Kriterien
  4. Erst danach gilt ein Fix als user-reif
- Mehrere frühere Agent-Claims (Pixel/Lens) zum `daily / 09:00`-Bug waren False Positives; neue operative Regel: **User-Screenshot / user-visible evidence schlägt interne Agent-Claims**.
- `daily / 09:00`-Bug Forensik verschärft:
  - `task-detail-modal.tsx` setzt lokalen Schedule-State bereits auf leer zurück (`scheduleKind=''`, `scheduleValue=''`), daher ist reiner Modal-State **nicht mehr alleinige Root Cause**.
  - Neue belastbare Hypothese: Wert kommt zumindest bei betroffenen Fällen aus den Task-Daten (`GET /api/tasks/[id]`) bzw. serverseitiger Schedule-Normalisierung, nicht nur aus lokalem UI-State.
- Board-Zombie-Cleanup durchgeführt:
  - Stale Test-/Probe-Tasks ohne `workerSessionId`, ohne `receiptStage`, ohne frische Aktivität wurden klassifiziert und die gelisteten Zombie-Tasks auf kanonische Terminal-Metadaten gepatcht (`status=canceled`, `executionState=done`, `dispatchState=completed`).
  - Danach blieben nur 2 aktive Real-Work-Tasks übrig; mehrere Zombie-Tasks projektierten erst nach vollständiger Terminal-Metadaten-Normalisierung nicht mehr als aktiv.
- Ein konkreter Inkonsistenzfall wurde isoliert und bereinigt:
  - Task `b74c2e6e-c0a8-483b-8039-6933da52719b` war `status=done` + `executionState=done`, aber `dispatchState=draft`.
  - Patch auf kanonische Terminal-Metadaten (`dispatchState=completed`, `dispatched=true`) korrigierte die Board-Projektion.
- Build-/Serve-Pfad bleibt als eigener Infrastruktur-Risikoblock relevant:
  - separater Board-Task erstellt: `Build-Pfad-Konsistenz und eindeutiger Live-Serve-Pfad prüfen` (`f6c1760c-1e32-4cff-846c-457b885ce385`).
  - Forge konnte Build-Root-Artefakte für `/home/piet/.openclaw/workspace/mission-control` zeigen, aber echte Laufzeit-/Bundle-Eindeutigkeit war zeitweise nicht vollständig beweisbar.
- Modelllandschaft zusammen mit Forge/Lens bewertet:
  - problematisch: `llama-3.3-70b-instruct:free`, Free-Modelle auf kritischen Rollen, `openrouter/auto` als Dauerlösung
  - robust(er): `claude-sonnet-4-6`, `gpt-5.4-mini`, `gpt-5.4`
  - Synthese: `gpt-5.4-mini` ist nicht pauschal übergewichtet, aber es gibt Konzentrationsrisiko bei mehreren Spezialagenten auf einem Modell; das größere strukturelle Problem ist `openrouter/auto` + Free-Modelle in kritischen Rollen.
  - Strategische Empfehlung festgehalten: Sonnet/Opus **nicht** als Default, sondern als Quality-on-Demand / Quality-of-Quality-Eskalationspfad; Sonnet als Review-/Quality-Layer, Opus nur für finale High-Stakes-Eskalation.
