# OpenClaw 3h Active Stability + Task Details Fix

Datum: 2026-04-24
Owner: Codex
Scope: Mission Control live stabilisieren, kontrollierte Mini-Last, clientseitige Task-Details-Exception fixen.

## Arbeitsmodus
- Aktiv statt passiv: Root Cause identifizieren und kleine Fixes umsetzen, wenn Live-Gates kippen.
- Kleine Worker-Tasks einzeln, nie parallel fluten.
- Keine breiten Rewrites, keine QMD-Architekturänderung, keine Provider-/Routing-Mutation.
- Mutationen nur gezielt und belegbar; lokale Scripts bleiben Dry-run default.

## Priorität 0
- Client-side Exception beim Task-Details-Klick reproduzieren, Root Cause isolieren, Fix testen und deployen.

## Danach 3h Ablauf
- Alle 5 Minuten: Health, Runtime-Soak, Pickup-Proof, Worker-Proof, Health-Reconciliation, Service-Status.
- Nach grünen Gates: kleine real-use Worker-Tasks einzeln starten.
- Nach jedem Task: Pickup/Worker/Health prüfen.
- Bei Rot: keine weiteren Tasks, Root Cause sammeln/fixen, erst dann weiter.

## Stop-Regeln
- `/api/health` degraded durch neue Recovery-Last.
- `pickup-proof.criticalFindings > 0`.
- `worker-reconciler-proof.openRuns > 0` bleibt nach Taskende hängen.
- neuer Claim-Timeout.
- Mission Control oder Gateway nicht active.
- Session-/Output-Guard kritische neue Aktivsignale.

## Statuslog
- 2026-04-24T09:51:15Z gestartet; UI-Details-Exception ist erster Root-Cause/Fix-Fokus.
- 2026-04-24T09:53:48Z Root Cause reproduziert: Task-Details-Klick erzeugte React Error #310. Ursache: `useSwipe()` wurde in `TaskDetailModal` nach `if (!open) return null` aufgerufen, wodurch sich beim Öffnen die Hook-Anzahl änderte. Fix: Hook vor den Early-Return verschoben. Regressionstest `tests/task-detail-modal-hook-order.test.ts` passed; `npm run typecheck` passed. Ein älterer Mobile-Layout-Source-Test fällt separat auf eine bestehende `href`-Erwartung für Lane-Jump-Buttons.
- 2026-04-24T10:00:35Z Zweiter Root Cause aus Browser-Probe: Taskboard-SSE replayte ohne Cursor alle historischen Board-Events und triggert dadurch Snapshot-Sturm. Fix: neuer Cursor-Resolver startet ohne expliziten Cursor am aktuellen Event-Tail; Reconnect-Cursor replayt weiterhin. Tests `tests/task-detail-modal-hook-order.test.ts tests/board-events-sse-cursor.test.ts` passed; `npm run typecheck` passed.
- 2026-04-24T10:06:52Z Deployed und live verifiziert: Build passed, `mission-control.service` und `openclaw-gateway.service` active. Browser-Flow `Taskboard -> Details` öffnet Modal ohne React PageError (`pageerror=0`) und ohne Snapshot-Fail-Sturm (`snapshotFailed=0`, `refreshWarnings=0`). Health/Pickup/Worker/Health-Reconciliation ok. Runtime-Soak blockiert aktuell nur durch Context active critical bei James Output-Caps (`context-active-critical-clear`).
- 2026-04-24T10:16:25Z Context-Budget-Proof-Fix umgesetzt und deployed: `memory/per-tool-bytes.log` wird fuer Tool-Peak-Baselines mit 1 MiB Tail gelesen, damit wiederholte historische Maxima nicht als neue aktive Peaks gelten. Regressionen: `tests/context-budget-proof.test.ts` 8/8 passed, `npm run typecheck` passed, Production Build passed, `mission-control.service` und `openclaw-gateway.service` active. Live: `/api/ops/context-budget-proof` jetzt `activeCriticalFindings=0`; `/api/ops/runtime-soak-proof` blockiert korrekt nur noch wegen echter aktiver Main-Gateway-Session-Lock (`agent-session-lock-clear`).
- 2026-04-24T10:19:47Z Operator meldet: Atlas arbeitet noch an einer direkten Anfrage; parallel wurde versehentlich Task `848f7fc5-1d7a-41f1-95fe-f677597a8a10` nach `main` dispatched. Entscheidung: keinen Canary starten. Read-only Proofs zeigen `pendingPickup=1`, `openRuns=1`, aber `criticalFindings=0`; Blocker ist die echte Main-Session-Lock. Naechster Schritt: ueberwachen, bis Lock frei wird, dann Pickup/Run/Heartbeat/Terminalstatus verifizieren.
- 2026-04-24T10:23:08Z Atlas hat den Task akzeptiert und eine Team-Welle dispatched: Forge `f6128939-8f27-4272-b17d-8da2ffe342f2`, Pixel `1ee76214-5e4d-43b7-b448-68db5934ee93`, Lens `e4d7e20e-afbd-4130-bef0-93d691bb893a`, Spark `d861181c-abf9-47ca-b826-4ed59976f1bc`, James `112cdb5b-d355-4717-b95c-300ce2c426ac`. Entscheidung: keine weiteren eigenen Tasks/Canaries. Live-Stresstest read-only ueberwachen: Pickup, Claim-Timeouts, Open-Run-Abbau, Terminal-Receipts.
- 2026-04-24T10:42:34Z Live-Stresstest-Abbruch sauber stabilisiert: James wurde terminal `done`; Atlas wurde vom Timeout-Guard terminal `failed`; Forge wurde ueber die API-Fail-Route terminalisiert; Pixel/Lens/Spark waren nach Gateway-Restart stale ohne Prozessbeleg und wurden lokal atomar terminalisiert, inklusive Run-Close. Gates danach: `/api/health` ok, `worker-reconciler-proof` ok mit `openRuns=0`, `pickup-proof` ok. Runtime-Soak bleibt bewusst blockiert wegen echter Output-Cap-Criticals aus der Team-Welle (`frontend-guru`, `main`, `spark`) und aktiver Main-Session-Lock.
- 2026-04-24T10:43:10Z Naechster dedizierter Fix-Fokus: nachhaltige Output-Cap-Erzwingung im OpenClaw-Gateway. Live-Befund: bestehende Guard-Konstante liegt bei 40k Zeichen und greift fuer Spark nicht ausreichend; aktuelle Criticals sind echte neue ToolResult-Peaks bis 226 KB, nicht mehr der vorherige Proof-Tail-False-Positive. Kein Gateway-Restart, solange aktive Session-Locks nicht eingeordnet sind.
- 2026-04-24T10:45:00Z Output-Cap Root Cause konkretisiert: `content.text` wird bereits gekappt, aber `toolResult.details.aggregated` persistiert grosse Exec-Ausgaben weiter in die JSONL-Zeile. Nachhaltiger Minimal-Fix ohne Bundle-Rewrite: `/home/piet/.openclaw/openclaw.json` setzt `agents.defaults.contextLimits.toolResultMaxChars=5000`; neuer systemd-Drop-in `output-caps.conf` setzt `PI_BASH_MAX_OUTPUT_CHARS=5000` und `OPENCLAW_BASH_PENDING_MAX_OUTPUT_CHARS=5000`. Gateway kontrolliert neu gestartet, Service active, Env-Caps sichtbar. Health/Worker/Pickup ok; Runtime-Soak blockiert nur noch wegen alter aktiver 30-Minuten-Context-Peaks.
- 2026-04-24T10:53:01Z Stale-no-process-Reconcile als nachhaltiger Pfad kodifiziert: `worker-run-reconciler` erzeugt jetzt eine eng begrenzte Action `fail-stale-open-run-without-process-evidence` nur fuer `in-progress` + `executionState=stalled-warning` + stale open run + kein frischer Heartbeat/Prozessbeleg. `scripts/worker-reconciler.mjs` bleibt Dry-run default und execute-mutiert nur gezielt per `--task-id`. Tests `tests/worker-run-reconciler.test.ts` 10/10 passed, `npm run typecheck` passed, Production Build passed nach kontrolliertem Stop/Start von `mission-control.service`. Live: health ok, worker proof ok, pickup proof ok; Runtime-Soak blockiert nur noch wegen alter Context-Peaks und Main-Discord-Session-Lock.
- 2026-04-24T10:57:28Z Canary-Script zielagentenbezogen gehaertet: ein Lock auf Agent A blockiert nicht mehr pauschal alle anderen Agenten, aber der betroffene Zielagent bleibt blockiert. Tests `tests/runtime-soak-canary-script.test.ts tests/worker-run-reconciler.test.ts` 15/15 passed, `npm run typecheck` passed. Forge-Canary `a23c8622-ce7e-46a9-8433-f095923c4edc` gestartet, nach Claim/Progress sauber `done`; danach `openRuns=0`, `pendingPickup=0`, Context active critical 0. Forge-Session-JSONL max line 2.982 Bytes, Output-Caps wirken.
- 2026-04-24T11:04:39Z Main-Canary `7ff01ecd-25e9-44e0-a3c7-5d75489e60eb` lieferte negativen Real-Use-Befund: Claim-Timeout bei `main`, danach aktiver Main-Session-Lock. Gezielt via `scripts/pickup-reconcile.mjs --execute --task-id 7ff01ecd-25e9-44e0-a3c7-5d75489e60eb` gecancelt; 1 Task mutiert, 1 Placeholder-Run geschlossen. Finales Gate direkt danach: `/api/health` ok, `worker-reconciler-proof` ok, `pickup-proof` ok, `context-budget-proof` activeCritical 0. Runtime-Soak blockiert nur noch durch frischen Main-Lock; keine weiteren Canaries starten.
- 2026-04-24T11:18:39Z Main-Canary-Rootcause nachhaltig eingegrenzt und gefixt: Der Claim-Timeout schloss Task/Run, stoppte aber nicht sicher die bereits gestartete gateway-owned Main-Session; diese Session konnte danach einen Child-Canary-Task erzeugen. Der unbeabsichtigte Child-Task `4e2d6ccb-e509-4b70-b924-0ae3546049f2` wurde admin-canceled. Fix: Runtime-Soak-Proof nutzt jetzt agentenspezifische Cooldowns (`main` 10 Minuten, Worker 2 Minuten) und liefert `canaryCooldownMs`; Canary-Prompt verbietet explizit Replacement-/Child-Canaries nach Cancel/Timeout/Claim-Failure. Gates: `npx vitest run tests/runtime-soak-proof.test.ts tests/runtime-soak-canary-script.test.ts` 13/13 passed, `npm run typecheck` passed, Production Build passed, Services active. Live-Proof: Main `canaryEligible=false`, `reason=agent-session-cooldown`, `canaryCooldownMs=600000`; Forge `selectedAgentCanExecute=true`; Worker/Pickup/Health ok, Context activeCritical 0.
- 2026-04-24T11:24:06Z Main-Canary-Sicherung nochmals verschaerft: `scripts/runtime-soak-canary.mjs` blockiert `--agent main` jetzt auch nach sauberem Proof, solange nicht explizit `--allow-main` gesetzt ist. Tests erweitert: Main ohne Override blockiert, Main mit Override + sauberem Proof erlaubt. Gates: `tests/runtime-soak-proof.test.ts tests/runtime-soak-canary-script.test.ts` 15/15 passed, `npm run typecheck` passed, Production Build passed. Live-Dry-run: `--agent main` `selectedAgentCanExecute=false`, `--agent main --allow-main` bleibt wegen aktuellem Cooldown false, `--agent sre-expert` true. Health ok, Worker ok (`openRuns=0`, `criticalIssues=0`), Pickup ok (`pendingPickup=0`, `criticalFindings=0`), Services active. Context nur degraded durch zwei aktive Warnings, keine aktive Critical.
- 2026-04-24T11:25:03Z Monitoring um weitere 2h verlaengert. Operator erweitert Ziel auf echte sequenzielle Canary-Tests, bis Worker und Main gruen sind. Vorgehen: alle Worker einzeln, nach jedem Task Health/Worker/Pickup/Runtime pruefen; Main/Atlas zuletzt und nur mit explizitem `--allow-main`. Keine parallelen Canaries.
- 2026-04-24T11:52:45Z Dedizierte Rootcause-Analyse fuer Lens-Canary umgesetzt: Forge und Pixel wurden sequenziell gruen (`done/result`), Lens zeigte Claim-Timeout und 409-Receipt-Konflikte. Root Cause: der Canary-Task beschrieb den Pending-Pickup-Claim-Contract nicht explizit; Lens versuchte non-terminale `accepted` Receipts ohne gueltige `workerSessionId`, waehrend `/api/tasks/{id}/receipt` bei ungebundenem Pending-Pickup `dispatchToken` plus non-empty `workerSessionId` mit erlaubtem Prefix verlangt. Fix: `scripts/runtime-soak-canary.mjs` schreibt jetzt einen `Claim-Receipt-Contract` in die Canary-Beschreibung und zeigt ihn im Dry-run; agentenspezifisch z.B. `workerSessionId "agent:efficiency-auditor"`. Tests: `tests/runtime-soak-canary-script.test.ts tests/runtime-soak-proof.test.ts tests/pending-pickup-claim-token.test.ts tests/receipt-run-binding-regression.test.ts` 22/22 passed, `npm run typecheck` passed, Production Build passed. Live-Re-Test Lens `3bc580cc-a936-4776-8d5f-f3fdc45f2ba6`: accepted mit `workerSessionId=agent:efficiency-auditor`, danach `done/result/completed`; Worker/Pickup/Health ok.
